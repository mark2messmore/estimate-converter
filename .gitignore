node_modules/
dist/
*.log
.env
.env.local

# Tauri build output
src-tauri/target/

# Signing keys (NEVER COMMIT)
update-keys.key
code-signing.pfx

# Release runbook with embedded signing password — local only
RELEASE-GUIDE.md

# Temp files
tmpclaude-*

# Release installers (uploaded to GitHub releases, not repo)
*.exe

# Cloudflare Workers
.wrangler/
.dev.vars
